# Ignore hidden files except the .gitignore and .gitmodules
.*
!.gitignore
!.gitmodules

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.ex
bin/

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
